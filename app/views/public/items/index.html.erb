

<div class="container">
	<div class="row">
		<div class="col-xs-3">
			<h4>ジャンル検索</h4>
		</div>
		<!-- サイドバーでジャンル検索 -->
		<h2>商品一覧</h2>

			<% @items.each do |item| %>

				<%= link_to item.name, public_item_path(item.id) %>
				<%= item.price %>

			<% end %>
	</div>
</div>
	
